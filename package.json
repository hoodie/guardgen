{
    "private": true,
    "workspaces": [ "library", "bin" ],
    "scripts": {
        "build:lib": "yarn --cwd library build",
        "build:bin": "yarn --cwd bin build",
        "build": "yarn build:lib && yarn build:bin",
        "test": "yarn --cwd library test"
    }
}
